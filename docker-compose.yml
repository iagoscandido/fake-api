version: "3.8"

services:
  db:
    image: postgres
    container_name: db-fake_api_store
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: "1234"
    volumes:
      - "./src/main/resources/sql/:/docker-entrypoint-initdb.d"